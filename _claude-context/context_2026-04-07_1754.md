# BTI Voice — Claude Context Snapshot
**Saved:** 2026-04-07 at 17:54
**Project folder:** `Talkingvet Help/` (user's selected workspace folder)
**User:** Paul (paulm@businesstechnologyinsight.com)

---

## What This Project Is
**BTI Voice** — a shared VOIP/SMS desktop app for the BTI/Talkingvet team. Built with:
- **Frontend:** React (Vite) + Electron (frameless window, ~320px wide like Zoho Voice)
- **Backend:** Node.js/Express + PostgreSQL + Socket.io
- **SMS/Calling:** Twilio (browser-based WebRTC calling via Twilio Client)
- **Deployment:** Railway (`bti-voice` service + Postgres service)
- **Live URL:** `bti-voice-production.up.railway.app`

The app is a narrow, mobile-style single-pane UI. NO "resolve" mechanic — conversations are permanent communication history. All tabs use single-pane navigation (list → detail → back button).

---

## Project File Structure
```
Talkingvet Help/
├── bti-voice/
│   ├── client/               ← React frontend (Vite)
│   │   └── src/
│   │       ├── App.jsx               ← Root: manages notifOpen, activity, seenAt, default tab='dialpad'
│   │       ├── api.js                ← All API calls incl. activity: () => request('/activity')
│   │       ├── ThemeContext.jsx      ← Dark/light theme, useColors() hook
│   │       └── components/
│   │           ├── TitleBar.jsx      ← Bell icon with unread badge, unreadCount + onBellClick props
│   │           ├── BottomNav.jsx     ← 5 tabs: Contacts, Messages, Calls, Dialpad, Settings
│   │           ├── ConvList.jsx      ← Custom AgentDropdown (no native <select>), flex:1 full width
│   │           ├── ChatPanel.jsx     ← Back button, NO resolve button, date separators, agent hint
│   │           ├── NotificationsPanel.jsx ← Bell panel, unread=blue dot+highlight, read=opacity 0.5
│   │           └── tabs/
│   │               ├── SMSTab.jsx        ← Single-pane, no onResolve
│   │               ├── CallsTab.jsx      ← Logs + Voicemails subtabs, filter pills, groupByDate
│   │               ├── ContactsTab.jsx   ← Single-pane, derived from conversations
│   │               ├── SettingsTab.jsx   ← All colors from useColors(), no hardcoded colors
│   │               └── DialpadTab.jsx
│   └── server/
│       ├── index.js          ← Registers all routes incl. /api/zoho
│       ├── db.js             ← migrate() incl. zoho_contact_id + zoho_synced_at columns
│       ├── zoho.js           ← NEW: Zoho OAuth2 token refresh + findContactByPhone()
│       ├── auth.js
│       ├── seed.js
│       ├── socket.js
│       ├── .env.local        ← Template for local testing (NOT committed, placeholders only)
│       ├── routes/
│       │   ├── auth.js
│       │   ├── agents.js
│       │   ├── conversations.js   ← Returns ALL convs (no is_resolved=false filter)
│       │   ├── messages.js        ← Fires syncSMSToZoho() after outbound send
│       │   ├── calls.js           ← Fires syncCallToZoho() after call logged
│       │   ├── activity.js        ← UNION query: inbound msgs + all calls, 80 items
│       │   └── zohoSync.js        ← NEW: POST /log-call, POST /log-sms, GET /test
│       └── webhooks/
│           ├── sms.js        ← Fires syncSMSToZoho() after inbound SMS saved
│           └── voice.js      ← TwiML for inbound/outbound browser calls
├── _claude-context/          ← THIS FOLDER: context snapshots before compaction
├── Zoho CRM Integration Guide.docx  ← Full integration guide (12 steps)
├── TeamVoice-Build-Plan.docx
└── TeamVoice-Demo.html
```

---

## Railway Deployment State
- **Service:** `bti-voice` on Railway project `intuitive-compassion`, env `production`
- **URL:** `bti-voice-production.up.railway.app`
- **Deploy command:** `cd bti-voice && railway up --service bti-voice` (always specify service)
- **Current Railway env vars (only 3 set manually):**
  - `DATABASE_URL` — auto-provided by Railway Postgres
  - `JWT_SECRET` — set
  - `NODE_ENV` — set
  - **NO Twilio vars set yet** — app runs fine, just skips SMS/calling gracefully
  - **NO Zoho vars set yet** — Zoho sync skips silently if ZOHO_REFRESH_TOKEN is missing

---

## Twilio Account Situation
- Paul has TWO Twilio sub-accounts under one login:
  - **Talkingvet Dialer** (SID: `<TWILIO_ACCOUNT_SID — see Railway env>`) — existing production dialer, COMPLETELY SEPARATE, never touch this
  - **BTI** (SID: `<TWILIO_ACCOUNT_SID — see Railway env>`) — newly created, currently on Trial, needs billing upgrade
- BTI Voice is connected to NEITHER right now (no Twilio vars in Railway)
- Plan: use the BTI Twilio account for BTI Voice exclusively

---

## Zoho CRM Integration — Current State
- `server/zoho.js` and `server/routes/zohoSync.js` are written and in the codebase
- DB migration adds `zoho_contact_id` and `zoho_synced_at` to contacts table
- Call logging fires after `POST /api/calls/log`
- SMS logging fires after outbound send (messages.js) and inbound webhook (webhooks/sms.js)
- **NOT yet deployed to Railway** — Zoho code is in the files but Railway hasn't been redeployed
- SMS logs as a Note on the contact (fallback). If SMS-Magic API identified later, swap in Option A from the guide doc.

### Zoho API facts confirmed by live testing:
- Phone search is format-agnostic: `"3038790040"` finds contacts with `"+13038790040"` stored
- Calls module has native fields: `Who_Id`, `Call_Type`, `Call_Start_Time`, `Call_Duration`, `Call_Result`, `$se_module`
- No dedicated SMS module found in their Zoho CRM module list — SMS tab is a marketplace extension (likely SMS-Magic)
- COQL queries require a WHERE clause (discovered this the hard way)

### Zoho OAuth2 vars needed (not yet set anywhere):
```
ZOHO_CLIENT_ID=
ZOHO_CLIENT_SECRET=
ZOHO_REFRESH_TOKEN=
ZOHO_API_DOMAIN=https://www.zohoapis.com
```
Full setup steps are in `Zoho CRM Integration Guide.docx`.

---

## Local Test Plan (In Progress — Not Yet Executed)
The plan to test without touching production:
1. Upgrade BTI Twilio account out of Trial (add billing)
2. Buy a test Twilio number in the BTI account (~$1/month)
3. Install ngrok, run `ngrok http 3001`
4. Set test number webhooks to ngrok URL in Twilio console
5. Fill in `server/.env.local` → copy to `server/.env`
6. Run `node index.js` locally on port 3001
7. Hit `http://localhost:3001/api/zoho/test` to verify Zoho connection
8. Create test contact in Zoho CRM with a real phone number
9. Test inbound/outbound SMS and calls, verify they appear on Zoho contact

---

## Key Technical Decisions & Conventions
- **ALL colors** must come from `useColors()` hook — never hardcode colors. Dark palette: bg `#161b24`, panel `#1d2330`, surface `#252d3c`, accent `#4f9cf9`
- **No resolve mechanic** — conversations are permanent. `is_resolved` column stays in DB but frontend never writes to it
- **Fire-and-forget Zoho sync** — Zoho errors must never block user-facing responses. Always wrap in `setImmediate` + try/catch
- **Single-pane navigation** — all tabs use list → detail → back, never split pane
- **Notification bell** — localStorage key `bti_notif_seen_at` tracks read state. Unread = new activity since last bell click
- **Agent dropdown** — custom React component, never native `<select>` (OS styles override dark theme)
- **Default tab** = `'dialpad'`

---

## What's Pending / Next Steps
1. **Local test:** Execute the test plan above with the BTI Twilio account
2. **Identify SMS extension:** Go to Zoho CRM → Setup → Marketplace → All to find the SMS app name (likely SMS-Magic)
3. **Deploy Zoho code to Railway** once local testing passes: `railway up --service bti-voice`
4. **Add Railway env vars** for Twilio (BTI account) and Zoho once both are tested

---

## Docs Reference
- `Zoho CRM Integration Guide.docx` — 12-step guide: OAuth2 setup, call logging code, SMS logging (SMS-Magic API + Notes fallback), testing, troubleshooting
- `TeamVoice-Build-Plan.docx` — original build plan
