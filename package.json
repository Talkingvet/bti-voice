{
  "name": "bti-voice",
  "version": "1.0.0",
  "scripts": {
    "build":          "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start":          "node server/index.js",
    "build:electron": "npm install --prefix client && npm run build:electron --prefix client && cd electron && npm install && npm run build:win -- --dir"
  },
  "engines": {
    "node": ">=18"
  }
}
